(()=>{const c=document.getElementById('starry-canvas');if(!c)return;const x=c.getContext('2d');let w=0,h=0,d=1,stars=[];function resize(){d=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*d;c.height=h*d;c.style.width=w+'px';c.style.height=h+'px';x.setTransform(d,0,0,d,0,0);const n=Math.max(55,Math.floor((w*h)/15000));stars=Array.from({length:n},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.2+.2,a:Math.random()*.65+.15,s:Math.random()*.004+.001}))}function draw(t){x.clearRect(0,0,w,h);for(const s of stars){const a=s.a*(.65+.35*Math.sin(t*s.s+s.x));x.beginPath();x.fillStyle=`rgba(220,214,255,${a})`;x.arc(s.x,s.y,s.r,0,Math.PI*2);x.fill()}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();requestAnimationFrame(draw)})();
